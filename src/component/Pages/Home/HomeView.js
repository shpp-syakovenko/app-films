import React from 'react';
import PropTypes from 'prop-types';


const HomeView = ({
}) => {
  return (
    <div className="home">
      home
    </div>
  );
};

HomeView.propTypes = {

};

HomeView.defaultProps = {

};

export default HomeView;
